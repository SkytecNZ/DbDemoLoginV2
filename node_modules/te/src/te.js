
/**
 * 前端模板引擎TE (Template Engine)
 * Copyright 2013 Johnson. All rights reserved.
 *
 * 
 * @file 类smarty前端模板引擎
 * @author Johnson(zoumiaojiang@gmail.com)
 * @date 2013-12-22
 * 
 */

var te = (function (root) {

    /**
     * 存放解析的语法节点的信息类
     * 
     * @param {Object} opts 配置参数
     */
    function CacheTree(opts) {
        this.config(opts);
    }

    CacheTree.prototype.render = function (data) {

    };


    /**
     * 引擎类
     * 
     * @param {Object} opts 引擎配置参数
     */
    function Engine(opts) {
        this.config(opts);
    }

    Engine.prototype.complie = function (tplCode) {

    };

    Engine.prototype.config = function (opts) {

    };

    Engine.prototype.complie = function (tplCode) {

    };


    

    var tests = new Engine({
        leftTarget: '{%',
        rightTarget: '%}'
    });

    return tests;

}(this));